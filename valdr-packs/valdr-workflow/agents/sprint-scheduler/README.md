# Sprint Scheduler

Sprint Scheduler is the read-only scheduling agent used by sprint workflows. It may inspect tasks and return an ordered list; the workflow performs all state changes and launches.
